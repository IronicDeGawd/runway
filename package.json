{
  "name": "runway-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "ui",
    "shared",
    "cli"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w ui && npm i @swc/core && npm run build -w cli",
    "build:cli": "npm run build -w shared && npm run build -w cli",
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "@swc/core": "^1.15.11"
  },
  "version": "0.1.0"
}
